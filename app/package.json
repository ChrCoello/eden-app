{
  "name": "eden-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^6.4.3",
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "polylabel": "^2.1.0"
  }
}
